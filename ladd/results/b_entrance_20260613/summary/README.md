# LADD B-Entrance Diagnostics Snapshot 20260613

This directory contains lightweight summaries generated from `ladd/results/b_entrance_20260613/evidence/`.

Included artifacts: summary CSV, selected `results.csv`, `args.yaml`, `ladd_diagnostics.csv`, split-load manifests, initial validation metrics, and compact log extracts.

Excluded artifacts: checkpoints/weights (`*.pt`, `*.pth`), TensorBoard events, wandb, full logs, and full run directories.
